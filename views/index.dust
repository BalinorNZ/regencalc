<!DOCTYPE html>
<html>
  <head>
    <title>{title}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{title}</h1>
    <p>Welcome to {title}</p>
    <p>Base is {base} - Gain is {gain} - Cost is {cost}</p>
	<table>
		<tr><th>Level</th><th>Int</th><th>Regen</th><th>Seconds</th></tr>
	{#levels}
		<tr>
			<td>{level}</td>
			<td>{intel}</td>
			<td>{regen}</td>
			<td>{time}</td>
		</tr>
	{/levels}
	</table>

	<form action="/" method="POST">
		<input type="text" id="stat_base" name="stat[base]" value="{stat.base}" />
		<input type="text" id="stat_gain" name="stat[gain]" value="{stat.gain}" />
		<input type="text" id="stat_cost" name="stat[cost]" value="{stat.cost}" />
		<input type="submit" />
	</form> 

  </body>
</html>