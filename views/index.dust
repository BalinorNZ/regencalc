<!DOCTYPE html>
<html>
  <head>
    <title>{title}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{title}</h1>
    <p>Welcome to {title}</p>
    <p>Base is {base} - Gain is {gain} - Cost is {cost}</p>
	<table>
		<tr><th>Level</th><th>Int</th><th>Regen</th><th>Seconds</th></tr>
	{#levels}
		<tr>
			<td>{level}</td>
			<td>{intel}</td>
			<td>{regen}</td>
			<td>{time}</td>
		</tr>
	{/levels}
	</table>

	<ul>
	{#colors}
    <li>{.}</li>
  	{/colors}
	</ul>

  </body>
</html>